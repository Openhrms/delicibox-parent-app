import 'package:flutter/material.dart';
class ParentDonateTab extends StatelessWidget {
  const ParentDonateTab({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Donate planner – coming soon'));
  }
}
